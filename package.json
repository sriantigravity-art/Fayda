{
  "name": "market-nifty-oi-surge-ticker",
  "version": "1.0.0",
  "description": "Real-Time Indian Options OI Surge Ticker & Activity Radar",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "build:server": "npm --prefix server install && npm --prefix server run build",
    "build:client": "npm --prefix client install && npm --prefix client run build",
    "build": "npm --prefix server install && npm --prefix server run build",
    "build:all": "npm run build:server && npm run build:client",
    "start": "npm --prefix server start"
  },
  "keywords": ["options", "nifty", "banknifty", "oi-surge", "radar", "nse"],
  "author": "",
  "license": "ISC"
}
