{
  "installCommand": "npm install",
  "buildCommand": "npx vite build client --outDir dist",
  "outputDirectory": "client/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
